[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'
eclipse.project.name = appName + '-lib-Network'

dependencies {
    compile project(":Libraries/_Foundation")
    compile "com.esotericsoftware:kryonet:$kryonetVersion"
}
